// Re-export the GET and POST handlers from the custom auth configuration
// These handlers will manage authentication-related API routes (like sign-in, sign-out, callback)
export { GET, POST } from "@/auth";
